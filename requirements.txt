# Orchestration & LLM (local Ollama)
# Pinned for Python 3.9 on Windows: langgraph 0.6 + langchain-core 0.3.x pulls uuid-utils
# with a broken wheel on some setups. Use 3.10+ and uncomment newer lines if preferred.
langgraph>=0.2.28,<0.3
langchain-core>=0.2.43,<0.3
langchain-ollama>=0.1.0,<0.2

# Data / optional scraping later
pandas>=2.0.0

# Testing
pytest>=8.0.0
